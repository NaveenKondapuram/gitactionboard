{
  "name": "gitactionboard",
  "version": "1.0.0",
  "description": "Git Action Board",
  "type": "module",
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "latest"
  }
}
